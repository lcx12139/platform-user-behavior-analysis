# 整理与验证记录

## 本次范围

日期：2026-09-09。继续使用现有GitHub仓库，不另建仓库。原版00—11号SQL保留路径；新增12—20号SQL、中文执行索引及详细报告。最新PBIX和四张用户截图只复制/移动，未修改二进制内容。未连接生产MySQL执行更新，未重跑近2,000万行清洗。

## 原稿保留与文件移动

- 原工作区文件完整备份至本地`.local/release-20260909/originals/`。
- 新增原始`sql/1.sql`—`9.sql`整理为12—19号正式脚本，原稿移到本地`archived/sql/`。
- 四个历史PBIX移到本地`archived/powerbi/`；最新原件另以中文名称复制到公开powerbi目录。
- 布局加工脚本及旧检查JSON移到本地归档，避免重新运行会改写PBIX的历史脚本。
- 四张截图移到`assets/dashboard/`并命名为01—04页，原字节哈希一致。
- 原findings/methodology/review_notes路径保留为中文导航或记录；有价值的原分析内容合并进中文报告。
- 没有删除原始/清洗CSV，没有修改MySQL配置、服务或数据目录。

## 指标修正与补充

- ARPPU始终使用正价格购买用户分母；基础行为漏斗继续保留全部价格。
- 弃用历史NTILE评分，使用后续确认的R/F固定阈值及M累计分布；原评分留档。
- 修复bi_overall_kpi总用户来源：由三类行为汇总改为全行为月画像按user_id去重。PBIX旧读数1,638,498仍保留并标注，文档最终值1,639,358；待Desktop刷新。
- 新增星期维度只读下钻、结果核对SQL和同公式动态分解视图。它们不是本次已在生产库运行的结果。
- 保留首次事件路径近似、Session空字符串、RFM近似订单和观察窗口边界。

## 已完成验证

1. 21份SQL按MySQL方言进行静态解析，没有解析错误。12号脚本的多索引ALTER语句被解析器作为通用命令保留，已对照原语句人工核查，不能声称它通过完整方言解析。
2. 两份Python编译通过；原版/整理版清洗在合成边界样本上的结果一致，验证了长ID、去重、缺失Session、非正价格、CRLF和防覆盖。
3. 最终RFM的七种分层在SQLite合成样本上验证了评分边界与CASE优先级；结构分解恒等式和全行为用户去重验证通过。这些检查不能替代MySQL生产执行。
4. 最新PBIX的ZIP结构与模型可以只读解码：13张小型汇总表，共62行，不含user_id、user_session、product_id明细。PBIX和四张截图均与原稿SHA-256一致。
5. 模型中的同期群、会话漏斗、RFM核心价值/复购以及首次用户汇总与确认结果核对；旧overall_kpi总用户冲突单独记录。
6. 可解析查询使用localhost MySQL和手工汇总，未发现硬编码数据库账号、密码或个人用户目录；模型参数为空。未把不透明二进制内容描述为绝对安全保证。
7. 提交前检查UTF-8、中文、Markdown链接、图片路径、体积、忽略规则及明显凭据模式；不将工具依赖或临时核验文件纳入仓库。

## 发布范围与未完成事项

计划公开代码、中文文档、清洗汇总、最新版约134KB的PBIX和四张真实截图。约5.44GB的原始/清洗CSV、数据库文件、凭据、IDE缓存、历史PBIX、工具依赖与迁移记录不上传。

需要在Power BI Desktop更新总用户来源、纠正第2页标题/图例、中文化原有显示标签并刷新截图。详见[看板核对与待办](看板核对与待办.md)。这些是已有看板的待修正项，不影响本次对代码、文档和真实原稿的整理发布；不能宣称看板已经全部修正。

本文件不硬编码自身提交哈希；准确发布哈希以Git历史及交付说明为准。
